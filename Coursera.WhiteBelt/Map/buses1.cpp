#include <iostream>
#include <string>
#include <map>

using namespace std;

int main() {
	size_t q;
	cin >> q;

	for (int i = 0; i < q; ++i) {
		string command;
		cin >> command;

		if (command == "NEW_BUS") {

		}
		else if (command == "BUSES_FOR_STOP") {

		}
		else if (command == "STOPS_FOR_BUS") {

		}
		else if (command == "ALL_BUSES") {

		}
	}
}